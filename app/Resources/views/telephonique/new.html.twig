{% extends '::layout/layoutAdmin.html.twig' %}

{% block body %}
    <h1>Telephonique creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit"  class="btn btn-success" value="Ajouter" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('telephonique_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
