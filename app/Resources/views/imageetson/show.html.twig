{% extends '::layout/layoutAdmin.html.twig' %}

{% block body %}
    <h1>Imageetson</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ imageetson.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ imageetson.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ imageetson.description }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ imageetson.prix }}</td>
            </tr>
            <tr>
                <th>Disponible</th>
                <td>{% if imageetson.disponible %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('imageetson_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('imageetson_edit', { 'id': imageetson.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
