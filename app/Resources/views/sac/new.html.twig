{% extends '::layout/layoutAdmin.html.twig' %}

{% block body %}
    <h1>Sac creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" class="btn btn-success" value="Ajouter" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('sac_modifier') }}" class="btn btn-info">Retour a la page Modifier</a>
        </li>
    </ul>
{% endblock %}




