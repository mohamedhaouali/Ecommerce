{% extends 'base.html.twig' %}

{% block body %}
    
    <div class="container">
        <div class="row">
            <div class="span3">
                {% include '::modulesUsed/navigationsac.html.twig' %}
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    {% include 'UtilisateursBundle:Default:modulesUsed/utilisateursConnecte.html.twig' %}
                {% else %}
                    {% include 'UtilisateursBundle:Default:modulesUsed/utilisateurs.html.twig' %}
                {% endif %}
                {{ render(controller('GestionProjetBundle:Panier:menu')) }}
            </div>
   
  <div class="span9">
                
                    <ul class="thumbnails">
                    {% for sac in sacs %}
                            <li class="span3">
                                <div class="thumbnail">
                                    <img src="{{asset('uploads/'~sac.path)}}" alt="DevAndClick" width="300" height="150">

                                    <div class="caption">
                                        <h4>{{sac.titre}}</h4>
                                        <p>{{sac.description}}</p>
                                        <p>Prix : {{sac.prix}}DT</p>
                                        <p>{% if sac.disponible %}<font color="green">Disponible</font>{% else %}<font color="red">Epuise</font>{% endif %}</p>
                                        

                                      

                                    </div>
                                </div>
                            </li>
                        {%endfor%}
                    </ul>
                       <div class="navigation">
    {{ knp_pagination_render(sacs) }}
</div>  
                </div>   


   

        </div>
                    
 
    </div>

</div>


{% endblock %}
        
            
               
            















































